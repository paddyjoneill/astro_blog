---
interface Props {
	href: string
}

const {href} = Astro.props

const { pathname } = Astro.url;
const isActive = href === pathname || href === pathname.replace(/\/$/, '');
---

<a href={href} class={`text-2xl text-black ${isActive ? "font-semibold" : ""}`}>
	<slot />
</a>
