---
import { Image } from "astro:assets";

interface Props {
  backgroundImage: any;
  title: string;
  pubDate: string;
}

const { backgroundImage, title, pubDate } = Astro.props;
---

<div class="h-64 overflow-hidden relative">
  <Image
    class="block h-64 w-full object-cover m-0"
    src={backgroundImage}
    alt="background"
    width="1024"
    transition:name="image"
  />
  <div class="absolute inset-0 bg-neutral-900 opacity-75"></div>
  <div class="absolute inset-0 flex items-center justify-center">
    <h1 class="text-3xl font-bold text-white" transition:name="title">
      {title}
    </h1>
  </div>
</div>
