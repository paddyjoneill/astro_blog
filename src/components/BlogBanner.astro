---
import { Image } from "astro:assets";

interface Props {
  backgroundImage: any;
  title: string;
}

const { backgroundImage, title } = Astro.props;
---

<div class="h-64 overflow-hidden relative">
  <Image
    class="block h-64 w-full object-cover m-0"
    src={backgroundImage}
    alt="background"
    width="1024"
    transition:name="image"
    loading="eager"
  />
  <div class="absolute inset-0 bg-neutral-900 opacity-75"></div>
  <div class="absolute inset-0 flex items-center justify-center">
    <h1 class="text-3xl font-bold text-white mx-4" transition:name="title">
      {title}
    </h1>
  </div>
</div>
